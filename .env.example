# Speech2Text — Umgebungsvariablen
# Kopiere diese Datei nach `.env` und trage den echten Schlüssel ein.
# .env NIEMALS committen (siehe .gitignore).

# OpenAI API-Key — https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...
